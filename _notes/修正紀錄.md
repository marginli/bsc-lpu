# LPU 專題課程：修正紀錄

**這份檔案不是教材，不會被部署到網站上。**
給教材設計者看的：製作過程中寫錯、被抓出來、然後改掉的每一條。
教學網頁上只留更正後的結論，不留「我本來寫錯了」的敘述——那對沒有背景知識的學員只會造成混亂。

建立於 2026-09-02，最後更新 2026-09-03。
涵蓋 `what-is-lpu.html`（PART 1）與 `how-to-draw-lpu.html`（PART 2）。

---

## 一、反覆出現的四種錯誤型態

看完下面的清單，錯誤集中在四類。**做下一個專題時，這四類要當檢查表用。**

### 型態 A：把自己的推論寫成論文的話

出現 **9 次**，是最常見也最危險的一類。表現形式包括：
- 直接寫「論文說……」但論文沒說（第 9、10、17 條）
- 寫「論文沒有說明 X 的理由」，暗示有個謎，其實 X 是定義使然（第 11 條）
- 用「為什麼要 X？因為……」的問答結構，答案是自己編的（第 20、21 條）
- 引用圖說裡的一句描述，當成論文的定義或規定（第 17 條）

**檢查方法**：任何「論文說 / 論文規定 / 論文的假設是」，都要能在 `pdftotext -layout` 出來的全文裡 grep 到原句，並記下行號。查不到就改寫成自己的話，或刪掉。

### 型態 B：數字沒有回頭算過

出現 **6 次**。包括縮寫對照的計數（第 6 條）、記憶體估算（第 18 條）、熱點體素數（第 19 條）、以及在同一輪對話裡剛寫進去就錯的統計（第 25 條）。

**檢查方法**：頁面上每一個數字都要有一行可重跑的程式對應。寫進 HTML 之前先在終端機印一次。

### 型態 C：圖與圖說不一致

出現 **3 次**。手挑 16 個腦區卻寫成「各腦區」（第 16 條）、曲線只畫到轉折點之前（第 28 條）、兩張腦擠在同一張 PNG 導致標籤錯位（第 1 條）。

**檢查方法**：產圖的程式若有 `show = [...]` 這類寫死的清單，圖說必須明說是抽樣。畫參數掃描曲線時，範圍要涵蓋到曲線轉向為止。

### 型態 D：結論其實是某個參數的產物

出現 **3 次**，是最晚才發現、也最傷的一類（第 26、27、28 條）。我們宣稱「用不同資料、不同分區、不同管線獨立重現了論文的結果」，但那個「重現」完全靠一個我們自己隨手設的門檻撐著。

**檢查方法**：任何「我們的結果跟論文一致」的宣稱，都要先把管線裡每一個自訂參數掃一遍，確認結論在合理範圍內不翻盤。**掃不出穩定區間，就不能說「重現」。**

---

## 二、PART 1（什麼是 LPU）

| # | 錯在哪 | 事實 | 型態 |
|---|---|---|---|
| 1 | 兩顆蕈狀體畫在同一張 PNG，`.panlab` 用 flex 平分容器寬度，右邊的標籤落在容器中線而不是右腦下方；圖例擠到邊緣 | 拆成兩張共用同一個 `bbox` 裁切框的 PNG，用 `.figpair` grid 排版，各自帶圖說 | C |
| 2 | 內文寫「左邊那張／右邊那張」 | 手機上面板是上下堆疊的，改用 A／B 標記 | C |
| 3 | 「一顆大約有兩千五百個」主詞不清，會被讀成一顆 Kenyon cell | 原文是 `MBs…are paired neuropils composed of around 2,500 Kenyon cells (KCs) **each**`，改成「每一側大約由兩千五百顆 Kenyon cell 組成」 | — |
| 4 | 「那不是兩個腦區在溝通，那是同一顆神經元的頭和尾」——這句話否定了論文自己的連結判準 | 連結的定義**就是**「同一顆神經元伸進兩個區」。改成把這個反問明白寫出來，承認資料完全相同，差別只在邊界劃在哪 | — |
| 5 | 「你需要一個獨立的、**與連線無關**的依據」（出現兩次） | LPU 的兩個條件都用到神經元。不能拿來定義邊界的是**連線表**（它預設了分區），不是「連線」 | A |
| 6 | 「同一個東西，換了縮寫：10 個」 | 實際是 **8 個**。10 是把 MB（一對三的拆分）和 OPTU→AOTU（Ito 正文從未列出這條對照）算進去湊的，又漏掉 LoP→LOP。改成列出全部 29 個名字分五類，並顯示加總 | B |
| 7 | 「沒有兩個 neuropil 合成一個 LPU」 | 論文有合併：`We divided a single MB into two neuropil regions…the entire MB was one LPU`、`the combination of the ellipsoid body (EB) ring and the two lateral triangles may be considered as one LPU`。而且這與它自己的前提 `Assuming an LPU is equal to or smaller than the boundary of an anatomically demarcated neuropil region` 相矛盾 | A |
| 8 | 「論文**一開頭**的假設是……」 | 這句話不在開頭。它是補充材料〈Defining an LPU〉的第一句（全文第 1778 行），以及正文引入 LPU 那一句（第 211 行，主詞寫的是 *an information processing unit*）。兩處都改成逐字引用 | A |
| 9 | 「Chiang 論文自己也指出 hub 一詞的混用風險」 | **完全沒有這回事。** grep `confus` / `ambigu` / `centrality` 全文 0 次 | A |
| 10 | 「LPU 的分區檔從來沒有公開發布過」 | 摘要寫 FlyCircuit 提供 `three-dimensional visualization of all single neurons, **brain-wide LPUs**…` | A |
| 11 | 「論文在建這張表時把所有 LPU-LN 排除掉。**論文沒有說明排除的理由。**」 | 這是廢話包裝成發現。連結的定義是「同一顆神經元伸進兩個 neuropil」，在地神經元本來就記不出跨區連結。整句刪除 | A |
| 12 | 把「分群跑出四個功能模組」當成 LPU 的貢獻 | 那是在 **58 個 neuropil** 的矩陣上做的，不需要 LPU。真正用到 LPU 的是神經束繪製（`we clustered neurons based on whether they follow a similar trajectory path linking specific LPUs`）。第 7 節改名為「LPU 到底被用在哪裡」，拆成「沒有用到 LPU 的部分／有用到 LPU 的部分」 | A |
| 13 | 第 8 節六條限制裡，有兩條（突觸解析度、配準誤差）不是 LPU 自己的限制 | 拆成 A 組（LPU 自己的）與 B 組（繼承自資料的）。另外 3.9 µm 是「地標點到標準腦中心」的距離，不是樣本之間的距離；補上 3.9 ± 0.4 與 1.1 ± 0.2（n = 10） | B |
| 14 | 中文句子裡混入英文單字「同時 connect 向所有人」 | 用 regex 掃 CJK 之間夾小寫拉丁字母找到的 | — |
| 15 | 「論文規定一條神經束至少由三顆神經元組成」 | 見 PART 2 第 17 條，同一個錯誤 | A |

**第 5、11 兩條是使用者直接下的禁令**：
> 「這句刪掉，因為這是我們自己無中生有的論證，這句之後的內容請避免這種自己無中生有的論證」

刪掉那一句之後，把它後面的內容整段稽核了一遍，又替換掉 7 處自創的說法。

---

## 三、PART 2（如何畫出 LPU）

| # | 錯在哪 | 事實 | 型態 |
|---|---|---|---|
| 16 | LN 普查圖的圖說寫「**各腦區**有多少……」，但 `fig_census` 裡 `show = [...]` 是手挑的 **16 個**腦區 | 全部 75 個腦區裡有 **28 個**有 LN 候選、47 個是 0。漏掉的還是大戶：ME_R 654、ME_L 653、LO_L 164、LO_R 159、GNG 74、LOP_R 59、LOP_L 49。圖改成完整普查（左：28 個非零，對數軸；右：47 個零） | C |
| 17 | 「論文對神經束的**規定**是：一條神經束至少由三顆神經元組成」 | 不是規定。那句話只出現在**圖 7D 與圖 S7 的圖說**：`Each tract is composed of at least three neurons and may actually link more than two LPUs`。真正的綁束演算法在〈Searching Neural Tracts〉，判準是**兩端平均終點位置相距 < 30 體素**且**總路徑長度相似度 > 60%**，沒有任何神經元數量的規則 | A |
| 18 | 「50 億個距離、約 **20 GB**」 | `scipy` 的 `pdist` 回傳 float64，5×10⁹ × 8 bytes = **37 GB**。20 GB 是 float32 的數 | B |
| 19 | 「上面那些腦區的熱點**各約三千個**體素」 | 實測 AL_R 3,131／AL_L 3,129／AVLP_R 3,330／AVLP_L 4,018／FB 1,885／**PB 458**。差快七倍 | B |
| 20 | 「為什麼用四分位？因為觸角葉裡有一百多顆 LN 疊著，某個小區域可能只有五顆……」 | 論文沒有解釋過這個選擇，那個例子是編的。改成實測（六區留下 25.0–25.2%，是四分位的定義使然），並補上原本漏掉的**代價**：比例固定，等於「這區到底夠不夠密」在這一步就被抹掉了 | A |
| 21 | 「『放大 4 倍』是在容忍誤差」——斷定了論文的意圖 | 論文沒給理由。而且漏了更要緊的：`with all voxel units enlarged 4 folds` 有**兩種讀法**（每方向 ×4／體積 ×4），**差 16 倍**。改成指出這個含糊，理由標明是我們的解讀 | A |
| 22 | 「論文說『多數 neuropil 都符合 LPU 的條件』，**走的就是前一條路**」——把推論寫成事實 | 補上依據（`In only one case (i.e., VLP), we found two LPUs in one neuropil`）並註明論文沒有逐區交代 | A |
| 23 | 「c > 2 的意思就是：中心至少比周邊**密一倍**」——會被讀成 +100% 還是 ×2 | 改成「中心的平均密度要在周邊的**兩倍以上**」。SVG 內同步改 | — |
| 24 | STEP 7 判定表把 hub 那列寫成「c > 2？否」 | 論文的 hub 是「**沒有 LN 族群**」（`without populations of LNs`）——沒有 LN 就根本算不出 c | A |
| 25 | 「19 對 22，其實相當接近」；而且列出的 8 個單側腦區裡，IPS_L／R、PLP_L／R 其實左右都有（屬於前面那 9 對），還漏了 GNG（74 顆） | 數字湊合而已：那 10 個裡 FB 233、PB 81、GNG 74 是大戶，其餘 **7 個**只有 1–2 顆；連那 9 對裡的 IPS 與 PLP 也各只有 1 顆。整段改成說明兩組數字不可比 | B |
| 26 | 「我們的 9.3% 與論文的 26% 不同，因為**我們的分區更細**；另外我們用**腦區**當單位，論文用 **LPU**」 | **兩句都不成立。**後者是前者的重述；而分區粗細實測只解釋 **1.8 個百分點**（併成 61 區只從 9.3% 升到 11.1%）。真正的主因是我們自己設的 **5 體素門檻**——改成 20 就到 24.1% | D |
| 27 | 「這張圖**獨立重現**了論文圖 5A 的正負號……這代表這個判準本身是穩的」 | 正負號也是那個門檻撐起來的。門檻 20 時 MB_CA_R 36、MB_ML_R 49、EB 101，「−」全變「＋」。只有 MB_PED 在各門檻下都是 0 | D |
| 28 | 門檻敏感度圖只畫到 t = 40，看起來單調上升 | 畫到 400 是**倒 U**，峰值 50.8% 在 t ≈ 61。而且 26% 這條線**被穿過兩次**（t ≈ 22 與 t ≈ 191）——「調到跟論文一樣」有兩個解 | C / D |
| 29 | `.plain` 的 `<div>` 用 `</p>` 收尾 | 用逐行 div-depth 掃描找到 | — |
| 30 | 「3×3×3 共 27 個體素的平均」沒說清楚是移動平均還是併格；而且我們自己的圖其實**做了真正的併格**（`B = 4`）卻沒揭露 | 論文是移動平均（`centered at the voxel r`，視窗重疊 18/27，格點不變）。我們的 `B = 4` 是為了控制距離矩陣大小的工程妥協，已在 STEP 1 明說 | A |

---

## 四、幾條沒有錯、但值得記下的判讀

這些不是錯誤，是查證過程本身值得留檔的部分。

- **圖 5A 不在 PDF 的文字層**。用 PyMuPDF 以 300 dpi 把第 6 頁算成影像才讀得到：22 個「＋」列（含 `Cal+MB` 與 `Lat Tri+EB`）、9 個「−」列（Cal、EB、FSPP、IDLP、Lat Tri、MB、Nod、OG、OPTU）。交叉檢查：29 個縮寫 ＋ 2 個合併列 ＝ 31 列。
- **41 = 19 對 ×2 ＋ 3 個中央不成對**，論文自己寫的。只有 22 個不同的名字。
- **58 有兩個**：矩陣的 58 個 neuropil，與 58 條 tract，是兩個不相干的數字剛好相同。
- **hub 的數量論文自己給了三個答案**：摘要六個、正文「四對」、圖 5A 上「−」有九個。
- **座標框**：FC12_warp 的一格不是一個真實微米。D03 的 affine 行列式 0.154，奇異值 0.626／0.605／0.406，所以 1 格約 0.4–0.63 個 FCWB 微米。**D06 資料說明裡寫的「2 µm 骨架接近度」很可能是 2 FC 格 ≈ 0.8–1.25 真實微米——這是使用者自己資料集的文件問題，留給他決定。**
- **著作權**：論文是 Elsevier「All rights reserved」，而 repo 是公開的。所有圖都從 CC0／GPL-3 的本機資料（FCWB 模板 ＋ FCWBNP 標籤）自己算，或自己畫成 inline SVG，一律不重製論文原圖。

---

## 五、工作方式上的兩條教訓

1. **`cd` 不會跨 Bash 呼叫保留。** 有好幾次工具呼叫因 `FileNotFoundError` / `not a git repository` 失敗。一律用絕對路徑，或在同一條命令裡明確 `cd`。
2. **先問清楚再動手。** 使用者每開一個新 PART 都先說「先不要動手，先討論」。這個順序讓 PART 2 的結構（七步驟 ＋ 兩件不在步驟裡的事）在寫之前就定了下來，省掉整段重寫。

---

## 六、寫過但從教學頁拿掉的內容

不是因為錯，是因為對學員來說是雜訊。**記在這裡以免日後又被加回去。**

### 2026-09-03 從 PART 2 的 STEP 0 移除

1. **〈回到分區粗細〉整節。** 內容是：左圖兩條曲線之間的垂直距離就是分區粗細的全部貢獻，同樣門檻 5 之下 75 區 9.3%、併成 61 區 11.1%，只多 1.8 個百分點；而沿同一條曲線往右走動輒二三十個百分點。結論句是「9.3% 這個數字主要在描述我們那個 5 體素門檻，不是在描述果蠅腦」。
   → 這個對比現在完全交給圖本身（兩條曲線 ＋ 圖說）承擔，正文不再重述。

2. **門檻改變會翻轉正負號的那張表。**

   | 腦區 | 門檻 5 | 門檻 10 | 門檻 20 | 門檻 30 |
   |---|---|---|---|---|
   | MB_CA_R | 0 | 1 | 36 | 208 |
   | MB_ML_R | 0 | 0 | 49 | 127 |
   | EB | 0 | 5 | 101 | 166 |
   | AOTU_L | 0 | 7 | 13 | 19 |
   | MB_PED_R | 0 | 0 | 0 | 0 |

   附帶的觀察：唯一在四個門檻下都是 0 的是 **MB_PED（蕈狀體柄）**——那是一束穿過去的軸突，本來就不該有在地神經元。這個觀察本身很漂亮，但撐不起獨立一段。
   → 為了不留下「我們獨立驗證了圖 5A」的錯誤印象，普查圖上方的綠框改成**先講條件再講結論**：「在我們用的判定門檻下（該區留下 5 個以上體素才算進入），這張圖的正負號跟論文圖 5A 對得上」，並明說那個門檻是我們自己設的、論文沒有。這樣不必敘述「我原本說錯了」，也不會誤導。

3. **PART 1 坑三不受此影響的說明。**（坑三的依據是論文正文與圖 5A 自相矛盾，與我們算出什麼無關。）
4. **「腦區 vs LPU 當單位」的討論。**（以 LPU 為單位比以 neuropil 粗的只有蕈狀體與 EB＋側三角兩處，會讓比例略升；但 LPU 不含 hub，完全待在 OPTU／Nod／OG／IDLP 裡的神經元不算 LPU-LN，又讓比例略降。兩邊都是小量，互相抵消。）

**共通的判斷標準**：這四段都是在回答「我們的數字為什麼跟論文不一樣」的**次要分支**。主線（門檻是主因、曲線是倒 U、比例門檻是比較像樣的代用品）留著，分支拿掉。

### 2026-09-03（第二次）STEP 0 整段改用比例判準重做

原本的 LN 代用判準是 D06 的 `n_LPU_touched == 1`，門檻「在某腦區留下 5 個以上的 2 µm 體素才算進入該區」。使用者指示改用比例判準、取 **f = 80%**。

**改動後的數字（整條管線都受影響）：**

| | 舊（體素門檻 5） | 新（比例 f = 80%） |
|---|---|---|
| LN 候選 | 2,664（9.3%） | **6,922（24.2%）** |
| 非零腦區 | 28／75 | **35／75** |
| AL_R 的 LN 數 | 113 | **723** |
| AL_R 五數綜合 | 0／5.1／14.0／24.0／46.1 | **0.6／51.4／92.9／136.5／206.6** |
| 熱點體素範圍 | 458–4,018 | **456–4,006** |
| 切割高度掃描 | 單調下降 | **先升後降**（高度 4 為 0，5–6 衝到十幾個，22 以上收斂到 1） |

**這次換判準連帶解決／改變的事：**

1. **24.2% vs 論文的 26%**——同一個量級，原本 9.3% 那個三倍落差整個消失。所以〈為什麼跟論文差三倍〉那一大段（倒 U 曲線、26% 被穿過兩次、峰值 t≈61）在頁面上**不再需要**，已整段移除。曲線本身的教學價值仍在，但它是在講一個我們已經不用的判準。
2. **圖 5A 的「−」變成穩健結論。** f 從 70% 掃到 100%，蕈狀體四塊合計都只有 0 或 2、結節全程 0、AOTU 最多 12 顆且 80% 以上掉到 0–1。不再像體素門檻那樣一調就翻。
3. **EB 仍然不穩**（56 → 11 → 0），而 EB 正是論文自己前後矛盾的那一個。這個不穩定本身反而是有意義的訊號。
4. **切割高度的敘述要改。** 舊版寫「曲線平滑下降，中間沒有明顯平台」，新資料下**每個腦區各自有平台，但平台高度不同**：高度 14–20 之間，AL 是 1、AVLP 是 2、FB 是 2–3、PB 是 2。論文說這六個都是單一 LPU，所以沒有任何一個高度能同時給出六個正確答案。

**觀察到但沒寫進教材的一條**：在高度 14–20 那個平台上，唯一穩定被切成兩塊的是 AVLP——而 Chiang 全篇唯一切成兩個 LPU 的正是 VLP。但 Ito 的 AVLP 不等於 Chiang 的 VLP（Chiang 的 VLP 在新系統裡被拆成 AVLP、PVLP、PLP），所以這只是引人聯想，不構成證據，沒有放上頁面。

### 2026-09-03（第三次）STEP 1 移除「移動平均 vs 併格」整個框

被移除的內容：解釋論文的 3×3×3 是**移動平均**（每個體素各有一個以自己為中心的視窗，相鄰兩格共用 27 格中的 18 格，格點數與格子大小都不變）而不是把 27 格併成一大格；佐證是 STEP 3 的 1% 門檻仍以原本的格子在數，真的併格的話體素總數會掉到 1/27；以及「格點解析度沒變、有效解析度變粗」的區分。

這一段是回答使用者當下的提問而寫的，內容正確，但對學員是額外的名詞負擔——公式下方的小字（「以 r 為中心的 3×3×3 ＝ 27 個體素的平均」）與示意圖右半（「中央那一格的值 ＝ 這 27 格的平均」）已經把該說的說完了。

**連帶處理**：下一個框原本寫「除了 3×3×3 <b>移動平均</b>，還先做了一次真正的併格」，「移動平均」這個詞的定義隨框一起消失，改成「除了論文那個 3×3×3 的平均」。

### 2026-09-03（第四次）名詞統一

使用者指出：「上四分位數就是第三四分位，請統一名稱，這樣才不會讓學員搞混。」逐組掃過整個 PART 2，找到四組混用。

| 混用 | 統一成 | 出現處 |
|---|---|---|
| 上四分位 ／ 第三四分位 ／ 上四分位數 | **上四分位**（第一四分位 → 下四分位） | STEP 2 正文、圖說、alt、目錄、流程圖 |
| 候選 LPU ／ 候選群 ／ 群 | **候選 LPU** | STEP 4 正文、流程圖第 4 步、gap 框 |
| 種子 ／ source ／ 起始的 source | **種子**（論文原字 <i>initial source</i> 只註明一次） | STEP 4 公式、SVG 標籤、gap 框 |
| 體素 ／ 格 ／ 格子 | **體素**（併格後叫**分析體素**） | STEP 1 白話框、SVG 四處、B=4 框、v(r) 分岔框 |

另外處理兩件：

- **圖裡的標籤也算。** `p2-fivenum.png` 的三條線標成 `Q1` / `median` / `Q_U (upper quartile)`——三個標籤三種命名風格。改成 `lower quartile` / `median` / `upper quartile Q_U`。
- **跨 PART 的對齊。** PART 1 通篇用「在地神經元」（13 次），PART 2 通篇用「LN」。在 PART 2 引用論文定義那一句加上中文名並宣告「本頁之後一律簡稱 LN」，把兩個名字綁在一起。

PART 1 單獨掃過，沒有同類問題（`四分位` 0 次；「中介單元」那一次出現在 2015 那篇的引文裡，屬於原文用字）。

**已寫進規範**：`specific_topics/CLAUDE.md` 新增第 6 節〈同一個東西只准有一個名字〉。

### 2026-09-03（第五次）STEP 3 補上「格點造成的平手」

使用者問：「這些體素都是人為整齊分割的，很多距離都會一樣，這要怎麼辦？」——這是我們自己跑了整條管線卻沒注意到的問題。

**量出來的規模**（右觸角葉的熱點）：3,128 個體素 → 4,890,628 對，但**相異的距離值只有 573 個**，平均每個值被 8,535 對共用；光是距離剛好等於 1 的就有 8,339 對。

**後果**：UPGMA 每一步挑「平均距離最近」的兩群，平手時 `scipy` 挑索引最小的一對——也就是**按輸入順序**。把同一批體素打亂順序重跑八次，右觸角葉在切割高度 6 得到 **12 到 16 個**候選 LPU。同一份資料、同一個參數。

**各連結法的實測**（AL_R，6 次打亂）：

| 方法 | h=6 | h=8 | h=10 | h=12 | h=16 |
|---|---|---|---|---|---|
| average（論文用的） | 12, 13, 16 | 5, 6, 7 | 3, 4 | 2, 3 | 1 |
| single | 1 | 1 | 1 | 1 | 1 |
| complete | 0 | 0, 1 | 8–13 | 11–15 | 6, 7 |

單一連結在高度 h 剪斷等價於「距離 ≤ h 的連通分量」，答案唯一、與順序無關——但熱點體素彼此緊鄰，一路串成一整塊，全部給 1。**可重現，但沒有分辨力。**

**頁面上的處理**：
1. `p2-cutheight.png` 從單線改成**帶狀**（實線＝8 次打亂的中位數，淡帶＝最小到最大）。帶最寬處（高度 5–6）光靠順序就差 6 個；高度 14 以上帶幾乎閉合。
2. 「切割高度 14–20」那張平台表改成把打亂算進去的範圍：AL_R 1、AL_L 1–2、AVLP_R 2、AVLP_L 1–2、FB 2–3、PB 2。原本寫的 1／1／2／2／2–3／2 少算了順序的變動。
3. `p2-clusters.png` 的圖說原本說「這個『六』完全取決於切割高度」，補上「也取決於體素的排列順序——高度 8 也可能得到 5 或 7 群」。
4. 收尾的含糊從三個變四個，缺口 6→7，需自行決定的事 8→9；首頁摘要同步。

### 2026-09-03（第六次）STEP 3 改成「兩條規則要一起用」

使用者指示：分群的兩個規則要一起強調——① 決定砍的高度、② 只留 >1% 的群。原本的寫法把 1% 塞在一句話的後半，讀起來像附註。

**改動**：
- 標題 `分群——以及那個沒有人給的參數` → `分群——兩條規則要一起用`（目錄與流程圖第 3 步同步）。
- 距離矩陣公式之後新增一個框，把兩條規則並列成表，並註明規則 ② 的原文出處、規則 ① 論文沒給。
- 新增一節〈兩條規則怎麼互相牽制〉，用右觸角葉的實測表呈現交互作用。
- 「樹要剪在哪裡？論文沒有說」→「規則 ① 的高度，論文沒有給」，後續段落一律改用「規則 ①／②」指稱。

**這次量出來的兩件新東西**：

1. **高度 4 的那個 0 不是「分不出群」。** 樹其實剪出 **50 群**，但最大的一群只有 116 個體素，沒過 125 的門檻，**一群都沒通過**。規則 ② 把整批刷掉了。

   | 切割高度 | 規則 ① 剪出 | 規則 ② 通過 | 最大群 |
   |---|---|---|---|
   | 4 | 50 | **0** | 116 |
   | 5 | 26 | 12 | 195 |
   | 6 | 12 | 12 | 397 |
   | 8 | 6 | 6 | 635 |
   | 10 | 3 | 3 | 1,567 |
   | 14 | 1 | 1 | 3,128 |

   高度 5 是兩條規則同時起作用的唯一一段（丟掉 41% 的熱點體素）；高度 6 以後規則 ② 就不再刷掉任何東西。

2. **規則 ② 的分母是整個 neuropil，不是熱點。** 論文原句是 *1% of the total voxel number **in the neuropil***。右觸角葉腦區內 12,511 個分析體素 → 門檻 125；而熱點只有 3,128 個。**所以「1%」實際上要求一群佔掉熱點的 4%。把分母誤寫成熱點數，門檻會鬆四倍**——這是重跑的人很容易踩的坑，已寫進頁面。

### 2026-09-03（第七次）補上「連結法」的解釋

使用者看不懂這句：「單一連結沒有平手問題——在高度 h 剪斷它，等價於『把距離 ≤ h 的體素連起來，看有幾塊連通的』。」

**問題出在頁面從來沒解釋過「連結法」是什麼。** 頁面一路用 UPGMA，卻沒說清楚 UPGMA 的「平均」是在講**兩群之間**的距離該怎麼定——這是一個沒被引入就直接使用的概念。對「沒寫過程式」的讀者，這一句幾乎不可能讀懂。

**補上的內容**（新的一節，接在「那怎麼辦」之後）：
1. 先問「兩群之間的距離是什麼」，指出群裡有很多體素、有很多條距離，要選一條——**這個選擇就叫連結法**。
2. 一張自繪 SVG，三個並排面板：單一（取最短）／平均（取全部的平均，論文指定的）／完全（取最長）。
3. 社團的比喻：「一個人認識就算同一群」／「平均要夠熟」／「每個人都要熟」。
4. 為什麼單一連結不怕平手：它等價於連通分量，鋪路順序不影響最後有幾塊；而平均連結一旦併了 A 和 B，新群到 C 的平均距離就變了，所以先併誰會改變後面每一步。
5. 為什麼不用它：實測表 —— 高度 0.9 給 3,128 群、1.0 給 2 群、1.5 以上給 1 群。**3,128 直接掉到 1，中間沒有可用區間。**

**驗證**：單一連結剪在高度 h 與「距離 ≤ h 的連通分量」在 h = 0.9／1.0／1.5／2／3／6 上逐一比對，數字全部相同。

**踩到的 SVG 坑（兩個）**：
- `<text>` 裡寫 `<b>` 不會粗體，內容會整段消失。要用 `<tspan font-weight="700">`。
- 要重複使用的圖形群組必須放進 `<defs>`，否則 `<use>` 之外還會在原位多畫一份。

已加進技術稽核：交稿前檢查 SVG 內是否殘留 `<b>`。

**（同日）連結法示意圖上緣被切。** 三個面板都寫成 `<g transform="translate(x,10)">` 裡放 `y="0"` 的標題——`<text>` 的 `y` 是**基線**，13px 的字會往上長出約 10px，頂端剛好落在 viewBox 外。改成 `translate(x,22)`、viewBox 高度 182→172。

寫了一支稽核：對每個 inline SVG 算出「最高的文字頂端」（`translate` 的 dy ＋ `text` 的 y − 一個字高），跟 viewBox 上緣比。六張圖現在最小餘裕 11px。已列進規範的技術稽核第 4 項。

### 2026-09-03（第八次）「切割高度」是距離不是次數，以及 h≥22 那段其實是算術

使用者問：「UPGMA cut=8 是指砍在合併 8 次之後的樹上嗎？」——不是。頁面寫「在樹的某個高度把它剪斷」，卻從沒說**那個高度量的是什麼**，又是「概念沒引入就使用」。

**補上的說明**：樹的高度軸量的是「這兩群合併時的距離」。剪在高度 8 ＝ 保留所有「距離 ≤ 8 才發生」的合併，比 8 高的都不做。

| 切割高度 | 已做掉的合併次數 | 剩下的群數 |
|---|---|---|
| 1 | 1,509 | 1,619 |
| 2 | 2,776 | 352 |
| 4 | 3,078 | 50 |
| **8** | **3,122** | **6** |
| 14 | 3,127（全部） | 1 |

右觸角葉 3,128 個熱點體素要全部併完需 **3,127 次**合併；剪在高度 8 已經做掉 **3,122 次**。而第 8 次合併發生在高度 **1.00**。

**順便抓到一個對圖的誤讀**（我自己寫的）。原本寫「22 以上六個區全部收斂到 1」，語氣像是找到了一個穩定答案。其實：

| 腦區 | AL_R | AL_L | AVLP_R | AVLP_L | FB | PB |
|---|---|---|---|---|---|---|
| 整棵樹的高度（根） | 13.1 | 13.7 | 21.2 | 19.4 | 22.0 | 20.8 |

**六棵樹的根都在 22 以下。**「高度 22 以上都給 1」只是「22 超過了每一棵樹的頂端」——是算術，不是資料在說這六個區各是一個 LPU。原本那句「而那時候整棵樹已經被剪得幾乎不分群了」講得太含糊，換成這張表。

這是型態 D（結論其實是參數的產物）的又一次：曲線右段看起來像個平台，其實只是超出了樹的高度。

### 2026-09-03（第九次）PART 2 全頁稽核（換 f=80% 與加入打亂之後）

自動稽核全過：標籤平衡、連結錨點、圖檔存在、中英夾雜、SVG 文字溢出 viewBox、SVG 內 `<b>`、名詞混用。
十四條英文引文逐條對回原文（含雙欄還原）——全部相符。

**發現的錯（都是換判準／加入打亂之後沒跟著更新的數字）**：

| 位置 | 頁面寫 | 實測 |
|---|---|---|
| STEP 3 掃描表 AL_R | 0 – 12 | **0 – 16** |
| STEP 3 掃描表 AVLP_L | 0 – 13 | **0 – 15** |
| STEP 3 掃描表 FB | 1 – 12 | **1 – 13** |
| STEP 3 danger 框 | 「也可以被判成 12 個」 | **16 個** |
| 收尾表 STEP 3 那列 | 「可以是 1 個、也可以是 12 個」 | **16 個** |

原因是那張表建立時只跑了一次（沒打亂），後來圖改成帶狀、表卻沒跟著改。**表頭也補上「（高度 4–25 × 八次打亂）」**，說清楚範圍的來源。

平台表（14–20）重算後全部相符：AL_R 1、AL_L 1–2、AVLP_R 2、AVLP_L 1–2、FB 2–3、PB 2。

**另外處理三處**：
- STEP 5「**為什麼**要去掉細胞本體與初級神經突？**因為**……」是規範第 3 節禁止的第三種變形（用問答結構包裝自編理由，論文從未給理由）。改成直述解剖事實。
- STEP 5 小標「第二個沒給的參數」→「又一個沒給的參數」（STEP 3 那邊沒有標成「第一個」）。
- 招募示意圖漏改的「最小**群**裡體積最小的 LN」→「最小的**候選 LPU** 裡最小的 LN」。

**教訓**：改了產圖程式（單線→帶狀）之後，只更新了圖說，沒回頭核對同一節的表格。**圖與表是同一份數字的兩種呈現，改一個就要核另一個。**

### 2026-09-03（第十次）指涉詞一律指名對象

使用者指出「而上面那張表已經顯示，高度一動，答案就從 0 跳到 12」沒寫清楚是哪張表。掃過全頁，共 **13 處**裸指涉。

**PART 2 改寫 9 處**，例如：
- 「上面那張表」→「上面〈兩條規則怎麼互相牽制〉那張表」，並補上是哪兩個高度（4 → 6）
- 「見上一節」→「見〈35 個腦區有 LN，論文數出 41 個 LPU〉那一節」
- 「下面那張圖」→「下面那張切割高度掃描圖」
- 「（見下方）」→「（見下方「整棵樹的高度」那張表）」
- 「上面那六個腦區」→「上面掃描過的那六個腦區」
- h4 標題「我們自己那張圖」→「我們那張 AL_R 密度圖」

**PART 1 抓到兩處更嚴重的——指向不存在的圖**（順手掃的，不在使用者指定範圍內但屬同類）：
1. 「四個功能模組……是把**上面那張** 58 個 neuropil 的矩陣拿去做階層分群」——**PART 1 只有四張圖，沒有任何矩陣圖**。那個矩陣是論文的圖 6A／6B，因版權沒有重製。讀者往上找只會找到全腦分區圖。
2. 限制那一節的「**這張圖**說的是『哪一區的神經元把纖維伸進了哪一區』」——同樣指的是論文的連線圖，頁面上沒有。

兩處都改成明說出處並註明「本頁沒有重製」。另把「資料來源同上圖／同前圖」展開成完整出處，「上面那張圖」→「上面那張全腦分區圖」。

**已寫成稽核**：四個 HTML 檔掃 `上面那張圖／下面那張圖／上面那張表／下面那張表／前面那張表／上圖／下圖／上述／前述`，現在全部 ok。列進規範技術稽核第 5 項，並特別註明要檢查「這張圖／那張圖」指到的圖是否真的存在於頁面上。

### 2026-09-03（第十一次）中位數的取法沒交代，而且取偶數次造成不可能的值

使用者問「實線是八次打亂體素順序的中位數，這裡的中位數是指什麼？」——頁面沒說清楚是**在哪個維度上**取中位數。

**正確的說法**：在**每一個切割高度上**，把體素順序打亂重跑 N 次得到 N 個答案，實線是這 N 個答案的中位數。不是跨高度、也不是跨腦區取的。

**查的時候發現一個真的錯**：跑 **8 次**（偶數），中位數是第 4、5 個的平均，所以會出現 **13.5、9.5** 這種值——而「候選 LPU 數」只能是整數，**那條線上的某些點是任何一次都沒有跑出來的答案**。

改成跑 **9 次**（奇數），中位數就是排序後的第 5 個，一定落在某一次真的跑出來的整數上。

**連帶更新的數字**（9 次的 min–max 比 8 次寬）：

| | 原（8 次） | 新（9 次） |
|---|---|---|
| AVLP_L 範圍 | 0 – 15 | **0 – 16** |
| FB 範圍 | 1 – 13 | **1 – 14** |
| PB 範圍 | 1 – 10 | **1 – 11** |
| 同一高度的最大變動 | 6 個 | **7 個**（AVLP_R） |

平台（14–20）重算後不變：AL_R 1、AL_L 1–2、AVLP_R 2、AVLP_L 1–2、FB 2–3、PB 2。
AL_R 在高度 6 的九次結果改成逐一列出：12、13、13、13、13、14、14、14、16。

**教訓**：只要圖上出現統計量（中位數、平均、四分位），就要在圖說裡寫清楚**是在哪個維度上算的、樣本是什麼**；而且**取樣次數用奇數**，免得中位數落在不可能的值上。已寫進規範。

### 2026-09-03（第十二次）STEP 4 示意圖下緣被切——先前的稽核只看上緣

使用者指出 STEP 4 第一張示意圖被切到。**上一輪寫的 SVG 稽核只檢查文字的「上緣」**，這次是**下緣**：三段 `<text class="cap" y="194">` 放在 `translate(x,26)` 的群組裡，基線落在 220、字底 223.2，而 viewBox 高度只有 210。改成 232。

**把稽核重寫成完整的幾何檢查**，存成 `scripts/svg_audit.py`：
- 對 `text` / `circle` / `rect` / `ellipse` / `path` 逐一算出邊界框，跟 viewBox 的**四個邊**比。
- `text` 的寬度用「全形 1 em、半形 0.55 em」估，並依 `text-anchor` 調整起點；字級從 `<style>` 的 class 或 `font-size` 屬性取。
- **巢狀 `<g>` 用堆疊掃描累加 translate**——第一版用非貪婪 regex 抓 `<g transform=…>(.*?)</g>`，遇到內層 `<g class="g">` 就提早結束，導致纖維曲線被歸到 dx=0，報了三個假警報。
- 含相對指令（h/v/l/c…）的 `path` 略過，不然 `M0 0h300` 會被讀成座標。

**自我測試**：把 viewBox 改回 210，稽核確實抓得到那三段文字；改成 232 後六張全過。

六張 PNG 逐一看過（普查、判準敏感度、密度場、五數綜合、切割高度、熱點分群），沒有被裁切的標籤。PART 1 沒有 inline SVG。

**教訓**：稽核只寫了一半比沒寫更危險——上一輪跑出「六張圖最小餘裕 11px」，看起來像全查過了，其實只查了一個方向。

### 2026-09-03（第十三次）「論文也沒有說為什麼要放大」寫得會誤導

使用者問：「從一個 neuron 開始慢慢放大才能最後決定這個 LPU 的範圍，論文裡真的沒提到嗎？」

**兩件事被我寫成一件。**

- **雪球本身的目的，論文有說。** 正文第四步就是「<i>recruiting LNs into each subdivision</i>」；補充材料寫被招募的神經元「<i>designated as **building blocks** for the candidate LPU</i>」，而 STEP 5 的邊界正是繞著這些神經元畫的。
- **沒說的只有「比對重疊之前為什麼要先把體素放大」。** `enlarged 4 folds` 全文（含補充材料）只出現**一次**，前後沒有任何解釋。

原句「論文也沒有說為什麼要放大」放在「放大 4 倍有兩種讀法」的段落裡，指的是後者，但單獨讀會被理解成前者——**等於在說論文沒交代招募的用意，那是假的**。

**改法**：
1. STEP 4 開頭補上論文自己的兩句引文，先講清楚這一步在做什麼。
2. 新增一段白話回答「為什麼不能直接拿 STEP 3 的那群體素當邊界」——因為 LPU 的定義要的是**一份神經元名單**，不是一團體素。
3. 把那句改寫成「論文有說整個招募在做什麼……沒說的是為什麼比對重疊之前要先放大」，並指出 `enlarged 4 folds` 全文只出現一次。

**教訓**：寫「論文沒說 X」時，X 必須窄到不會被讀成更大的範圍。這是規範第 3 節那張表的第二種變形的變體——不是把廢話當發現，而是**把一個窄的缺漏講成一個寬的缺漏**。

### 2026-09-03（第十四次）PART 2 全頁稽核（第三輪）

**找到的實質問題一個**：STEP 7 的「循環論證」講得太強。

原本寫：「要找神經束，得先有 LPU；要驗證 LPU，又要先有神經束。論文沒有交代這兩件事的先後。」

實際查證：**論文兩處寫得不一樣**。

| 出處 | 原文 | 節點是 |
|---|---|---|
| 正文 | *we clustered neurons based on whether they follow a similar trajectory path linking specific **LPUs*** | LPU |
| 補充材料〈Searching Neural Tracts〉 | *For a selected neuron linking two **neuropils**…paths within the two selected **neuropils** were removed* | neuropil |

**照補充材料的演算法，神經束是在 neuropil 之間找的，根本用不到 LPU，循環就不存在。** 原本只引了正文那句，把一個「兩處不一致」講成了「方法上的循環」——又是「把 X 講得太寬」。改成並列兩段原文，並建議實作照補充材料（那是唯一寫出可執行門檻的地方）。收尾表的 STEP 7 那列同步改寫。

**名詞**：新寫的那段白話裡出現一個「格子」（應為「體素」），已改。

**十個「論文沒有／沒說」的斷言逐條複驗**，其餘九條都成立且範圍夠窄。順帶確認論文裡另一個門檻（*predetermined gray level threshold, about 80-90% on histogram*）是骨架追蹤的二值化門檻，跟等值面無關，「等值面的門檻論文沒有寫」仍然成立。

---

**引文稽核腳本寫成 `scripts/quote_audit.py`**，過程中修掉自己四個假警報：

1. **剝標籤時用空字串**，相鄰儲存格的字會黏成一句（`…linking specific LPUsLPU`）。
2. **剝標籤時全部換成空格**，行內 `<b>` 會在標點前留下空白（`two neuropils , an average`），也會讓相鄰儲存格黏成同一個候選字串。
   → 正解：**區塊標籤換成換行、行內標籤直接拿掉**。
3. **雙欄還原用固定欄寬**（`l[:78]`）——欄界每行不同，切錯就對不上。
   → 正解：以 **3 個以上空白**當欄界切開，再分別串起左右欄。
4. **沒有接回被換行切斷的字**（`demar- cated`、`interre- gional`）。
   → 正解：`(\w)-\s+(\w)` 接回；`long-range` 這種沒有空格的不受影響。

修完之後 PART 1（7 條）與 PART 2（16 條）**全部對到，未對到 0**。

**教訓**：稽核腳本本身也要驗。前兩輪它報「全部 OK」，其實是把真正的引文誤判成 `??` 之後我人工放行，等於稽核形同虛設。

### 2026-09-03（第十五次）「放大所有體素」為什麼有用——使用者的反問促成的量測

使用者問：「把所有體素都一起放大應該沒有任何的作用吧？為什麼要這麼做？」

**這個反問是對的**，而且指出頁面上的敘述不完整。判準是「重疊超過體積的 50%」，是一個**比例**——整體等比放大，分子分母同時變大，比例不變，**確實毫無作用**。

原文 <i>with all voxel units enlarged 4 folds</i> 唯一說得通的讀法是**形態學膨脹**：格點位置不動，把每個被佔用的體素各自撐大，讓貼著跑卻不共格的纖維產生交集。原文的 <i>voxel **units** enlarged</i> 也支持這個讀法——放大的是體素這個單位，不是座標尺度。

**量出來的證據**（`scripts/dilation_check.py`，右觸角葉的 LN 候選抽 60 顆）：

| 每個體素向外長胖 | 有交集的配對 | 重疊率中位 | 最大重疊率 | 超過 50% |
|---|---|---|---|---|
| **0 格（不膨脹）** | 96.4% | 0.015 | **0.10** | **0.00%** |
| 1 格 | 99.6% | 0.092 | 0.35 | 0.00% |
| 2 格 | 100% | 0.238 | 0.68 | 5.9% |
| 3 格 | 100% | 0.398 | 0.90 | 35.2% |

**不膨脹時最大重疊率只有 0.10，沒有任何一對過得了 50%——雪球一顆都招募不到，STEP 4 當場停住。膨脹不是在微調結果，是在讓這個步驟成立。**

原本 gap 框的「兩個要注意的地方」改成四個：① 不能照字面理解成整體放大 ② 不膨脹就跑不動（附上表） ③ 「4 倍」的兩種讀法 ④ 目視挑種子。

**教訓**：先前只寫「放大應該是在容忍纖維之間的空隙……但這是我們的解讀」，把一件**可以直接量、而且量出來很戲劇性**的事，寫成了一句沒把握的猜測。**能量的就去量，不要停在「應該是」。**

### 2026-09-03（第十六次）我把「為什麼不膨脹就過不了門檻」的原因講錯了

使用者追問：「體素都是人為整齊切的，沒放大時，根本不可能有重疊吧？你是怎麼算的？」

上一則我寫的原因是：「**神經纖維在體素網格上是細的，兩條纖維就算貼著跑，也很少真的佔到同一格**」——**這是錯的**。實測完全相反：

| | 右觸角葉 723 顆 LN 候選、141,327 個相異體素 |
|---|---|
| 只被 1 顆神經元佔用 | 27,366（**19.4%**） |
| **被 2 顆以上佔用** | 113,961（**80.6%**） |
| 被 10 顆以上佔用 | 59,624（42.2%） |

中位數是 **7 顆神經元共用一格**，最擠的一格被 **46 顆**佔用。**共用體素是常態，不是例外。**

**為什麼會這樣**：一個體素比一條纖維粗得多。D06 的體素邊長是 2 個座標格（≈ 0.8–1.25 真實微米），而果蠅神經纖維直徑常在 1 微米以下——同一個立方格裡塞得下好幾條不同神經元的纖維。

**正確的原因**：膨脹要解決的不是「完全沒有交集」，而是「**交集太小**」。兩顆神經元的分枝各自鋪得很開，真正重疊的只是其中一小塊——不膨脹時任意兩顆的重疊率中位只有 1.5%、最大 10%，離 50% 的門檻很遠。

**驗算方式**（避免是我程式寫錯）：除了 mask 陣列的 `&` 運算，另用純 Python 集合對三顆神經元逐一算交集，結果一致（共用 140／174／160 個體素，各佔對方 3.7–4.3%）。

`scripts/dilation_check.py` 加上佔用統計，兩張表都能一鍵重跑。

**教訓**：上一則我「量了」重疊率，卻沒有量**造成低重疊的機制**，就順手補了一個聽起來合理的原因——那仍然是規範第 3 節禁止的自編論證，只是這次包在一堆真實數字中間，更不容易被發現。**量了一半的東西，比完全沒量更容易讓人放心。**

### 2026-09-03（第十七次）膨脹的對象是神經元，不是格子；而且論文沒說撐大誰

使用者問：「所以，膨脹的是體素，還是神經元？」

**答案：神經元。** 格點位置、格子大小都沒動；動的是「這顆神經元佔了哪些格」——原本只佔一條細線，撐大之後佔一條粗一點的管。論文寫 <i>all voxel units enlarged</i>，字面是「所有體素單位」，但格子全部放大等於什麼都沒做，**有作用的只有被神經元佔用的那些**。原本頁面寫「把每一個體素各自撐大」，字面沒錯但沒點出對象，已改寫。

**追問下去又挖到兩個沒交代的地方**：

**一、撐大的是 source 還是 target？** 論文完全沒說，而差別不小（撐大 2 格）：

| 撐大誰 | 重疊率中位 | 超過 50% |
|---|---|---|
| 只撐大 source | 0.238 | 5.9% |
| 只撐大 target | 0.227 | **19.8%** |
| 兩邊都撐大，分母用 target 原體積 | 3.26 | 93.6%（比例超過 1，**這個讀法不成立**） |
| 兩邊都撐大，分母用 target 撐大後體積 | 0.232 | 4.0% |

**「只撐大 target」的通過率是「只撐大 source」的三倍多。** 本頁前面那張表用的是「只撐大 source」，因為論文的句子是「target 有多少比例落在 source 內」。

**二、「4 倍」在格點上做不到。** 撐大 1 格，體積就已經是 **6.2 倍**（2 格 16.9、3 格 29.7、4 格 41.0）。所以「體積 ×4」這個讀法**在體素格點上根本無法實現**——最小的一步就跨過去了。

收尾的含糊表把「4 倍的兩種讀法」那列改寫成「膨脹的細節：撐大誰、撐大多少」，把兩件事併在一起，總數維持 4 個含糊／7 個缺口／9 件事。

`scripts/dilation_check.py` 加上這兩組量測，頁面上的每個數字都對得回腳本的預設輸出（抽 60 顆）。**中途發現頁面先寫的是我臨時跑 40 顆的數字，跟腳本預設不一致，已全部改成腳本的輸出。**

### 2026-09-03（第十八次）「一格憑什麼能屬於兩顆神經元」——問了三次才答到點上

使用者第二次追問同一件事：「你還是沒有回答我的問題：體素都是人為整齊切的，沒放大時，根本不可能有重疊吧？」

**前兩次我都在回答「為什麼纖維會落在同一格」（體素比纖維粗），沒有回答「一格憑什麼能同時屬於兩顆神經元」。** 使用者的心智模型是「體素像拼圖，一格屬於一個東西」——在那個模型下，重疊在定義上就不可能，講纖維多細都沒用。

**真正的答案**：格子不是被「分配」掉的。

**每一顆神經元是單獨造影、單獨追蹤骨架，再各自映到同一個標準腦的格點上**，因此各自產生一份體素名單。**從頭到尾沒有任何一步把某一格指派給某一顆神經元**，所以同一個格子編號會同時出現在很多顆神經元的名單裡。

比喻：723 張透明片各畫一條線，疊在同一張方格紙上。任何一格都可能有好幾張片子的墨水，因為方格紙沒有把格子分給任何一張片子。

**資料結構直接就是證據**（D06 全腦）：

| | |
|---|---|
| （體素, 神經元）紀錄 | 11,895,201 筆 |
| **相異的體素** | **3,365,959 個** |
| 平均每個體素被幾顆神經元登記 | 3.5 |
| 最擠的一個體素 | **67 顆** |

D06 存的就是一份配對清單，同一個體素編號在裡面重複很多次。**1,189 萬筆紀錄只落在 337 萬個相異體素上。**

**教訓**：使用者說「你沒有回答我的問題」時，多半不是答案不夠詳細，而是**答錯了問題**。要先把對方的預設找出來——這裡的預設是「一格只能屬於一個東西」——直接回答那個預設，而不是在自己的框架裡補充更多細節。**同一個問題被問第二次，就不要再補充，要換一個層次重講。**

### 2026-09-03（第十九次）「算的是體素的重疊還是神經元的重疊」——量的東西沒有被定義過

使用者接著問：「或者，你計算的是體素的重疊，還是神經元的重疊？」

**答案：兩顆神經元的重疊，但度量單位是體素。**

```
重疊率 = （同時出現在 A、B 兩份體素名單裡的格子數）÷（B 的格子數）
```

程式就是 `(D[a] & M[b]).sum() / M[b].sum()`，`M[i]` 是神經元 i 的體素遮罩。比的是兩顆神經元，體素只是它們的表示法。

**但這裡有個更要緊的區別，頁面之前完全沒說**：**這不等於「兩顆神經元碰到了」。** 同一格只表示兩條纖維穿過同一個立方格；一格邊長約 0.8–1.25 微米，格子裡的兩條纖維最遠可以相距一整條對角線（約 1.4–2.2 微米）。而化學突觸的間隙只有幾十**奈米**——差了近百倍。

**所以 STEP 4 量的是「兩顆神經元佔不佔同一塊地盤」，不是「它們有沒有接觸」。** 用在這裡是恰當的（要找的本來就是「纏在同一區域的那一群 LN」），但這個數字不能拿去當接觸或突觸的證據——那正是 PART 1 第 8 節談的限制，現在加了連結過去。

在 gap 框裡新增為第二點（六點）：先定義「重疊」是什麼，再談不膨脹跑不動。

**教訓**：從第十五則到這一則，我連續給了三個關於「重疊」的答案，卻**從來沒有先定義「重疊」是什麼**。使用者一路追問的其實是同一件事——這個量到底在量什麼。**先把量測的定義寫清楚，再談它的數值。**

### 2026-09-03（第二十次）種子的條件按字面挑不出任何一顆

使用者複述他對 STEP 1→4 的理解，其中一句是：「同一個 LN 可能部分在高密度、部分不在，但是**只要有部分在高密度**，這個就有機會被選為起始的種子。」

**這不是論文的字面**。原文是「<i>a smallest LN with its fibers **restricted within** the smallest candidate LPU</i>」——纖維要**完全侷限在**那個候選 LPU 之內，不是有一部分在裡面。

**但照字面做，一顆都挑不出來。** 量出來（`scripts/seed_check.py`，AL_R、切割高度 8、最小群 397 個體素）：

| 723 顆 LN 候選各有多少比例的體素落在… | 中位 | 最大 | 達到 100% |
|---|---|---|---|
| 最小的那一群 | 4.8% | 35.8% | **0 顆** |
| 最小群膨脹兩格＋填洞（放寬） | 12.4% | 50.9% | **0 顆** |
| 全部熱點體素 | 39.1% | 76.5% | **0 顆** |
| 整個右觸角葉 | 92.1% | 99.5% | **0 顆** |

**這不是資料的偶然，是結構決定的**：STEP 2 只留密度最高的 25% 體素，所以此刻的「候選 LPU」只是一堆高密度格子；而一顆 LN 的分枝必然同時鋪在密的地方和疏的地方。**「完全侷限在熱點裡」在設計上就不可能成立。**

**所以使用者的讀法（有一部分在裡面就算）反而是唯一跑得動的**——只是那個「一部分是多少」論文沒有給。頁面上把這一點寫成 gap 框的第六點，並把收尾表的 STEP 4 那列從「起始 LN 靠目視挑」改寫成「起始種子怎麼挑」，把兩個問題都寫進去。

**教訓**：使用者複述理解時，不要只挑「對／不對」回答。他複述的版本**跑得動**，論文的字面**跑不動**——這種時候要兩邊都講，並且說明為什麼會這樣。

### 2026-09-03（第二十一次）「每個體素向外長胖」少了限定

使用者指出表頭「每個體素向外長胖」應為「每個**含神經元的**體素向外長胖」。

第十七則已經在正文說清楚「被撐大的是神經元的形狀，不是格子」，但**表頭、公式、流程圖、以及另一處正文都還是舊寫法**——只改了論述，沒改標籤。掃過全頁的同類寫法，共五處：

| 位置 | 原本 | 改成 |
|---|---|---|
| 膨脹敏感度表頭 | 每個體素向外長胖 | 每個**含神經元的**體素向外長胖 |
| 體積倍數表頭 | 向外撐大 | **神經元的體素**向外撐大 |
| 流程圖第 4 步 | 體素放大 4 倍 | **神經元佔的**體素放大 4 倍 |
| 正文 | 把每個體素撐大 | 把**神經元佔的**每個體素撐大 |
| STEP 4 公式 | 把所有體素放大 4 倍 | 保留論文原話，但加一行小字註明「所有體素」是原文用語，實際放大的只有神經元佔用的那些 |

公式那一處刻意保留論文的字面（*all voxel units enlarged*），因為公式框的定位是「論文怎麼寫」；用小字把差異標出來，而不是改掉引述。

**教訓**：釐清一個概念之後，要把**同一份文件裡所有引用該概念的地方**一起掃過——尤其是**表頭、圖上的標籤、公式框**這些「不是散文」的地方。它們最容易在改寫時被跳過，而讀者掃讀時看的往往正是它們。已寫進規範。

### 2026-09-03（第二十二次）註解貼錯欄，而且沒說「超過 1」為什麼是問題

使用者問：「『比例超過 1，這個讀法不成立』，我看不出來哪裡比例有超過 1？」

**兩個毛病疊在一起**：

1. **註解貼在錯的欄位。** 那句話寫在「超過 50% 的配對＝93.6%」那一格後面，但「超過 1」講的是前一欄的**重疊率中位數 3.26**。讀者盯著 93.6% 找 1，當然找不到。已把紅字移到 3.26 那一格。
2. **只說「不成立」，沒說為什麼。** 讀者要自己想出「重疊率是比例、上限是 1」才看得懂。已補一段白話：

   > 「重疊率」是一個比例，最大只能是 **1**（＝B 的體素全部都落在 A 裡面）。這一列算出 3.26，等於說「B 有 326% 的體素落在 A 裡」——不可能。
   >
   > **為什麼會跑出這種數字：分子分母不是同一個東西。** 分子數的是「撐大後的 A 與撐大後的 B 共同佔到的格子」，分母卻用「撐大前的 B 有幾格」。B 撐大兩格之後體積變成 16.9 倍，所以共同的部分輕易就比原本的 B 大好幾倍。**要嘛分子分母都用撐大後的（第四列 0.232），要嘛都用撐大前的（不膨脹 0.015）——不能一邊撐大一邊不撐。**

**教訓**：表格裡的紅字註解，**要貼在它所指的那一格**，而且**要能自己解釋自己**。「這個讀法不成立」是結論，不是理由；讀者手上只有一張表，沒有我腦中的推導。這跟第十九則的「先定義量測再談數值」是同一件事的另一面——**數值出現異常時，要說明異常在哪、為什麼會異常。**

### 2026-09-03（第二十三次）6.2 倍被寫成像是通則

使用者問：「『實測撐大 1 格，體積就已經變成 6.2 倍』這應該只是用某個例子算出來的結果吧？不是 universal 的？」

**對。** 那是 AL_R 抽 60 顆 LN 的中位數，而撐大一格的倍率**完全取決於物體的表面體積比**：

| 形狀 | 撐大 1 格後體積 |
|---|---|
| 孤立的單一體素 | 7.0 |
| 一條一格寬的直線 | 5.0 |
| 實心立方體 4³ | 2.5 |
| 實心立方體 8³ | 1.8 |
| 實心立方體 16³ | 1.4 |
| 實心立方體 32³ | 1.2 |

**為什麼我們的神經元都落在 6 附近**：骨架在這個解析度下幾乎就是「一格寬的分枝曲線」，每一個體素都在表面上，所以倍率夾在「直線 5.0」與「孤立體素 7.0」之間。分枝點與斷點往 7 推，筆直的段落往 5 拉。三個腦區各抽 60 顆實測：AL_R 4.3–6.6（中位 6.2）、FB 5.9–6.6、PB 5.9–6.7——**在這份資料上很穩，但不是通則**。

頁面上把 6.2 的來源標進表頭（「我們抽的 60 顆 LN 的中位數」），並新增參考形狀對照表與說明。`scripts/dilation_check.py` 加上倍率分布與參考形狀的計算。

**教訓**：「實測」兩個字不會讓一個數字自動變成通則。**寫出一個量測結果時，要一併寫出它是在什麼樣本、什麼條件下得到的，以及換個對象會不會變。** 這次是自己資料裡的中位數被寫成像是幾何常數。前面第十九則要求「先定義量測」，這一則補上另一半：**也要定義量測的適用範圍。**

### 2026-09-03（第二十四次）STEP 5 去掉初級神經突的理由，從一句斷言變成有證據

使用者問：「為什麼要去掉它們的初級神經突？」原本頁面只有一句斷言式的說明（「初級神經突只是把本體接到工作區的一條線」），沒有出處也沒有量測。

**論文在這一步沒有給理由**（原句就是 <i>after removing their primary neurites and soma</i>，沒有下文）。但理由在解剖學上清楚，而且可以量。

**解剖事實**：果蠅的神經細胞本體不在纖維網（neuropil）裡，它們排在腦的**表層**；初級神經突是從表層伸進纖維網的那一段，只是通勤路線。論文自己的圖說給了例子：某群神經元「<i>Their cell bodies are located in the ventral brain, and their primary neurites (arrows) project dorsally along the brain midline</i>」——本體在腹側，初級神經突沿中線一路往背側走。

**量出來的證據**（`scripts/neurite_check.py`，AL_R 723 顆 LN 候選、1,297,426 個體素，依離本體的路徑深度分段）：

| 深度 | 體素數 | 落在 AL_R 外 | 落在任何腦區外 |
|---|---|---|---|
| **0–5（最靠近本體）** | 4,271 | **63.2%** | **61.7%** |
| 5–10 | 5,647 | 39.0% | 37.4% |
| 10–20 | 22,581 | 15.2% | 13.2% |
| 20–35 | 100,280 | 7.9% | 4.9% |
| 35–65（中段） | 614,497 | 8.0% | 5.3% |
| 65–100（末梢） | 549,195 | 17.1% | 13.0% |

**最靠近細胞本體的那一段有六成以上不在任何腦區裡**——它就在細胞本體所在的表層。留著它，密度場會在腦區外多出一團，等值面被拉出去。

**另外查到**：論文在別處也排除細胞本體——FlyCircuit 的空間分布分析「<i>the soma was excluded</i>」，圖 S2 圖說也註明「<i>The soma was excluded</i>」。所以**排除細胞本體是這套資料的慣例；初級神經突則是 LPU 這一步才額外去掉的**。

原本那一句改寫成一個獨立小節〈為什麼要去掉細胞本體與初級神經突〉，先聲明論文沒給理由，再給解剖事實、論文別處的例子、以及量測。

### 2026-09-03（第二十五次）「兩者都沒有 LN」被我當成合併的理由，其實邏輯漏了一半

使用者問：「『兩者都沒有自己的 LN 族群，所以都不能各自成為 LPU』——這個是成為 LPU 的理由？」

**問得對。** 「都沒有 LN」是各部分**不能單獨成立**的理由，不是「合起來就成立」的理由。我把論文的邏輯抄了前半段。

**完整的邏輯是：LN 是相對於「哪一塊區域」定義的。** 同一顆神經元，在細的分區下不是 LN，在粗的分區下可能就是。

蕈狀體正是如此：**Kenyon cell 的樹突在萼、軸突在葉**。把萼和葉當兩個區，KC 就是跨區的投射神經元，兩邊都不算它的 LN——所以兩邊都沒有 LN 族群。**但把整個蕈狀體當一區，這 2,500 顆 KC 就全部變成它的 LN 族群。**

所以「切開後兩邊都沒有 LN」不是「這裡沒有 LN」，而是「**切錯地方了**」——該有的 LN 就跨在那條切線上。

**量出來的證據**（`scripts/merge_check.py`）：

| | 切成四塊 | 合併成整個蕈狀體 |
|---|---|---|
| 右側 | 萼 1、柄 0、垂直葉 0、水平葉 0 | **596** |
| 左側 | 四塊全是 0 | **619** |

中央複合體是同一回事，論文明寫「<i>Each EB LN gives dendrites at ipsilateral lateral triangle and axons surrounding the center ring</i>」。實測 EB 從 11 顆增加到 18 顆（再加 GA 是 24 顆）——方向一致，幅度遠不如蕈狀體。三組都併回去，全腦 LN 候選從 6,922 增加到 8,143。

**另外釐清**：論文對蕈狀體其實給了**兩條**理由，分別對應 LPU 的兩個條件——條件一是 LN（切開沒有、合起來有），條件二是「<i>KCs in an MB share similar long-range connectivity patterns</i>」。我原本把兩條擠在一格裡，讀起來像同一條。真正撐起「合併」這個動作的是條件一。

**教訓**：抄論文的因果句時，要問「這個 *because* 到底在支持哪一個結論」。「A 不成立」與「A∪B 成立」是兩個不同的結論，論文用一句話帶過，我就照抄，結果那一格的「理由」根本不成理由。**因果句要拆開驗證，不能整句搬。**

### 2026-09-03（第二十六～二十八次）使用者指定的三遍全頁檢查

**第一遍（機械稽核與數字重算）**——修 5 處：
- **「體素」全頁用了 76 次卻從未定義**。已在 STEP 1 補上定義，並順便宣告「格子／一格」是同義詞（原本稽核報的名詞混用，其實是白話裡的刻意用法，缺的是那句宣告）。
- 兩處裸指涉「前面那張表」→ 具名為「膨脹敏感度表」「合併案例表」。
- 目錄與章節標題兩處不一致。
- 通過：標籤平衡、連結錨點、圖檔 alt、中英夾雜、SVG 幾何、計數一致性（缺口七＝3＋4、九件事＝7＋2）、五支腳本重跑後 38 個關鍵數字全數相符。

**第二遍（引文與歸屬）**——20 條引文全對到，83 句「論文…」逐句查完，修 4 處：
- **AVLP 的歸屬錯了**：我寫「論文說這六個區都是單一 LPU」，但 AVLP 屬於 Chiang 的 VLP，那正是全篇唯一被切成兩個 LPU 的腦區。連帶必須處理「AVLP 在平台上給 2、VLP 也被切成 2」這個巧合——已明白寫出兩個 2 不是同一件事（Ito 把 VLP 重劃成三塊）。
- **上一輪改出來的自相矛盾**：新寫的合併邏輯說 MB 靠「KC 跨在萼與葉之間」，下方紅框卻還留著舊說法「靠 KC 連結樣式相似」。
- 「<i>populations</i> 是複數的族群」是過度解讀，那個複數指的是「可能有多個族群」。
- **導言承諾「每一步都先給論文的原式」**，但 STEP 4、5、7 論文根本沒給式子。
- 順帶補上 **STEP 7 唯一缺少的白話框**（稽核發現九節裡只有它 formula 0 且 plain 0）。

**第三遍（讀者視角通讀）**——修 6 處，全是流程與冗餘：
- **STEP 3 開頭直接給距離矩陣公式，沒說這一步要做什麼**。補上目的句。
- **「格點造成距離重複」離它的用途（平手）隔了 23 行**。在該段末補上具名的前向指引。
- **「一個關鍵分岔」（只有一群就是 LPU）被放在連結法之後**，位置錯了——那是兩條規則的直接延伸。已移到規則表之後。
- 「我們實際驗過這個等價關係」沒給驗證範圍，補上 h ＝ 0.9／1／1.5／2／3／6。
- STEP 4 第一點同一個論證講了兩次，刪掉重複的後半。

**這三遍的共同教訓**：第一遍抓機械錯、第二遍抓歸屬錯、第三遍抓的全是**結構問題**——目的沒先講、鋪陳與用途隔太遠、區塊放錯位置。**前兩種靠腳本抓得到，第三種只能真的從頭讀一遍。**

### 2026-09-03（第二十九次）PART 4 實跑抓到 PART 2／PART 3 的兩個錯

跑 PART 3 的十四條指令時，指令 4 的驗收表對不上，追出兩件事：

1. **PART 2 的 v(r) 不是它說的那個東西。** PART 2 寫「我們那張圖用的是重複註冊次數」，但程式 `np.add.at(f,(iz,iy,ix),1.0)` 數的是**每一筆（體素, 神經元）紀錄**——一顆神經元的多個 2 µm 體素併進同一個分析體素會被重複計入，平均 1.44 筆。既不是纖維條數，也不是相異神經元數，是第三個東西。
   影響有限（兩種定義 Spearman 0.998、熱點重疊 94%、平台值完全相同），但描述必須改。

2. **PART 3 指令 4 的驗收表數字錯了。** 寫「熱點約 20 萬、149 GB」，實際是 172,170、110 GB。原本用「粗體素 × 64」估，而粗體素是部分填滿的。

兩處都已更正。**這正是「驗收表比指令本身更有用」的實例**——沒有那張表，這個錯會一路傳下去。

### 2026-09-04（第三十次）PART 5 第一步：十六支整合成三個檔案

**改正的錯**：PART 4 的頁尾與正文都寫「共十支程式」，實際是**十六支、733 行**
（`c09b`、`c09c`、`c10_11`、`c12_13`、`c14_estimate`／`c14_run`／`c14_figure`
都是後來補的，寫那句話的時候還沒有）。兩處已改成十六支。

**整合的結果**（`part5/`）：

| | PART 4 | PART 5 |
|---|---|---|
| 檔案 | 16 支 | `run_lpu.sh`＋`run_lpu.py`＋`lpu_lib.py`＋`expected.json` |
| 行數／敘述 | 733 行／934 個敘述 | 1,724 行／1,032 個敘述 |
| 載入 `labels_fc_1um` 的地方 | 10 支 | 1 支 |
| `GX=GY=512` | 9 支 | 1 處（`Config.d06_grid`） |
| 寫死 `/home/wanjuli` | 14 支 | 1 處（`Config` 預設值，可被 CLI／環境變數覆寫）|

行數變多而敘述只多 10%，多出來的是 `check()`、`verify()`、argparse 與 docstring
——都是原本沒有的功能。管線本身的敘述數是減少的。

**驗證方式**：重跑一遍，把 16 份 JSON、8 個 `.npy`、2 個 CSV 與 7 張 PNG
逐一與 `part4/out/` 比對。**全部相同**（只有兩張圖差幾十位元組，因為標題把
en dash 換成 ASCII 連字號，以及 c07 的標題文字微調）。

**可攜性也是驗過的**：把四個檔案（84 KB）複製到一個空目錄，只給它
`run_lpu.py pack` 打包出來的 39 MB 資料，重跑，**JSON 與 PNG 全部逐位元組相同**。

**幾個當初沒想到的事**：

1. **資料壓縮率遠超預期。** 原本打算為了「搬機器」做一份裁切過的小樣本示範資料。
   實測發現整批 277 MB 用 `np.savez_compressed` 只剩 **38.6 MB**（標籤體積壓到 **0.6%**，
   因為它幾乎全是零與大片同值）。裁切樣本這個計畫因此整個不必要——直接搬全套就好，
   而且不會有「示範資料算出來的數字跟真的不一樣」這種麻煩。`_load_array()` 讀得懂
   `.npz`，另一台機器不必先解開，代價只有 0.4 秒。

2. **`c07` 的六個診斷欄位差點被整合掉。** `n_pairs`、`n_distinct`、`at6`、`h4_*`
   在原本的 `c07_sweep.py` 裡是寫死給 AL_R 的特例，重構時很自然會當成雜訊刪掉。
   但那幾個數字（4,896,885 對距離只有 557 種相異值）PART 4 的頁面引用過。
   **重構時「看起來像雜訊的特例」要先查有沒有被引用。** 已用 `detail=True` 參數留回來。

3. **`cut_sweep` 加了 `detail` 開關而不是一律計算。** `np.unique(np.rint(D**2))`
   在 ME_L（1.15 億對距離）上會再複製一份 0.9 GB。指令 7 只跑六個小區所以無所謂，
   指令 14 要跑 ME_L，一律計算會把峰值從 2.5 GB 推上去。

4. **實測峰值 2.5 GB**，不是先前估的 2.2 GB。差別在 `linkage` 的內部複本。

**一個刻意的設計**：主程式跑到指令 9 會**停下來**，因為那一步在這份資料上
沒有可用的操作區間（20 種參數組合非 1 即 723）。要往下跑必須明確加
`--force-recruit`。理由是——會安靜產出垃圾的管線，比會停下來說話的管線危險。

### 2026-09-04（第三十一次）PART 5 第二步：整頁重寫，順手拆掉舊骨架裡三件已經不成立的事

`python-lpu.html` 在第一步之前一直是大綱骨架。重寫時整頁丟掉重來，
下面三件是**刻意拿掉、不要再加回去**的：

1. **兩個「還沒驗證」的警告框。** 原文是「本頁所有程式碼都尚未在真實資料上跑過，
   執行時間、記憶體用量、以及能不能重現論文的 41 個 LPU，全都是未知數」。
   → 現在三項都有實測值（166 秒、2.5 GB、8 單一 LPU／13 多候選／54 候選 hub），
   骨架時期的免責聲明留著只會誤導。

2. **`scikit-image`（`marching_cubes`）與 `trimesh` 的套件清單。**
   → 實作後沒有用到任何一個。等值面是用 `ndimage` 的門檻加填洞做的。
   套件清單現在是實測的三個：`numpy`、`scipy`、`matplotlib`。
   `part5/README.md` 原本也寫了一句「早期骨架曾經提議裝這兩個套件」，一併拿掉——
   那是敘述自己改正過什麼，屬於 `_notes` 的東西。

3. **「照 BSC 的慣例，AI 在過程中犯的錯與被推翻的結論要一併寫進來，不要修飾成
   一路順利」這一句。** 那是 EP 系列（`online_course/`）的規矩，寫進 PART 5 是
   把兩套規範搞混了。專題課程的規矩相反，見 `specific_topics/CLAUDE.md` 第 2、12 節。
   **這一句如果哪天又冒出來，就是這條規矩又被搞混了一次。**

另外拿掉的還有骨架時期那個 `lpu/` 九個模組的目錄提案
（`io_am.py`／`voxelize.py`／`hotspot.py`／…）。實際整合出來是三個檔案，
不是九個模組——**切成九個模組會讓「同一件事寫在哪裡」重新變成九個地方**，
正好是這次重構要解決的問題。

**這一頁最後的骨架是五個問題**（參數在哪／資料怎麼進來／哪一段做哪一步／
怎麼證明沒寫錯／撐不住時會不會停），不是七個步驟。
理由：七個步驟 PART 2 已經講過了，PART 5 再講一次只是換成程式碼重述；
而五個問題是**換一份 AI 寫的程式也能照用**的東西，那才是這一頁該留給學員的。

**順手改的兩處**：`index.html` 的 PART 3／4／5 三個按鈕還掛著 `soon`
（虛線框、標題灰字），三部分都已完成，拿掉；首頁進度框改成「五個部分都已完成」。

**兩個過期數字**（都是把先前一次的量測直接沿用，沒有重量）：
`part5/README.md` 寫 `lpu_lib.py` 555 行（實際 552）、整條 167 秒／指令 14 95 秒
（重跑實測 166／94）。已更正。

### 2026-09-04（第三十二次）PART 5 全頁複查：同一類錯（文字說的與頁面實際呈現不符）共九處

使用者指出「藍底標起來的那七行」其實沒有被標起來（`pre .hl` 的透明度 .24
在深色底上等於沒有），並要求把整個 PART 5 再掃一遍。掃出來的是**同一類**：
**文字在描述頁面，但沒有回頭數過頁面。**

| # | 寫的 | 實際 |
|---|---|---|
| 1 | 「先講三個詞」（標題／目次／內文三處） | 四張名詞卡（函式、類別、模組、參數） |
| 2 | 「十四條指令，一條一個函式」 | 十二個函式——**而頁面就印著只有 12 條的 `ORDER`** |
| 3 | 「藍底那七行」 | 擷取漏掉 `rng_seed` 與切割高度四行，且沒標示省略 |
| 4 | 「掃過一億四千萬筆紀錄」 | 11,895,201 筆（差 12 倍） |
| 5 | 「1.7 億個格子」 | 181,660,076（1.8 億） |
| 6 | 行數 733 對 1,721 | 733 只算 16 支 `.py`，1,721 含 `.sh` 與 JSON，兩邊範圍不同 |
| 7 | `expected.json` 擷取的兩條「說明」 | 與檔案裡的字串不同（我自己縮寫了） |

**最嚴重的是第 8、9 兩處，跟上面不同類：**

8. **我在程式碼引文裡自己加了三個註解，而且兩個是錯的。**
   `self.ix / iy / iz` 三行在 `lpu_lib.py` 裡**沒有註解**，是我為了這一頁加的，
   寫成「左右／前後／上下」。正確的是**左右／背腹／前後**——
   PART 4 的軸序表白紙黑字寫著 axis 0 ＝ 前後、axis 1 ＝ 背腹、axis 2 ＝ 左右，
   而 `iz` 索引 axis 0、`iy` 索引 axis 1。**同一個 repo 裡的兩頁直接互相矛盾。**
   → 教訓：**往引用的程式碼裡加註解，等於自己寫了一句沒有出處的話**，
   要跟正文的引文一樣查證。已改正，並在 `lpu_lib.py` 補上正確的註解，
   讓頁面與原始碼指向同一個來源。

9. **那一段的理由是自編的。** 我寫「果蠅腦左右最寬，所以最寬的那個軸就是左右」。
   PART 4 的指令 1 實際用的是**三組解剖關係反推**（兩側視葉重心差 668 格且幾乎
   全落在同一軸、蕈狀體萼對食道下神經節、觸角葉對萼），不是「最寬」。
   典型的第 3 節第五種變形：**用「因為⋯⋯」包裝一個聽起來合理的自編理由**。

**程式裡也有兩個數字錯**（同一批複查抓到）：
`lpu_lib.py` 的 `Brain` 註解寫「1.7 億個 int16，約 174 MB」——
實際 dtype 是 **uint8**、元素 1.8 億。174 MB 反而是對的，因為 uint8。
**「數字對得上」不代表「說明對得上」**：int16 × 1.7 億 是 347 MB，
兩個錯剛好互相抵銷成一個對的檔案大小，所以三次量測都沒抓到。

**這次複查加進稽核的兩條**：
- 頁面上凡是「N 個／N 行／N 張」的說法，都要對回頁面裡真正的元素數
  （`<div class="term">`、`<span class="hl">`、`<tr>` 的出現次數）。
- 程式碼引文要跟原始檔逐字比對；**加註解也算改動**。

### 2026-09-04（第三十三次）PART 1–4 照 PART 5 的方式複查

使用者要求把其他四個 PART 也掃一遍。掃法同第三十二次：數量宣稱對回頁面元素、
數字重算、引文逐字比對、跨頁一致性。

**PART 1：沒找到問題。** 41 ＝ 19×2＋3、75 ＝ 34×2＋7、58 ＝ 29×2、43＋6 ＝ 49、
舊名字表 5＋8＋1＋1＋14 ＝ 29 全部自洽；「當心兩個 58」那個提醒本來就在；
兩處指向論文原圖的地方都明說了「本頁沒有重製」。這一頁先前被掃過三遍，看得出來。

**PART 2：一處錯，而且是第二十九次「已更正」但其實沒改的那一處。**

第二十九次的紀錄寫「兩處都已更正」——實際上只有 PART 4 寫了更正說明，
**PART 2 本身那句話原封不動**：「上面那張 AL_R 密度圖用的也是後者（重複註冊次數）」。
PART 4 已經證明它數的是紀錄筆數。這句今天才真的改掉。

→ 教訓：**「已更正」要指名改了哪個檔案的哪一句**。只寫「已更正」，
下次複查時沒有東西可以對。以後這份紀錄裡的「已更正」一律要附上被改的原句。

順帶實測補上兩個數字（`part5` 的資料重算，兩種定義並排跑）：

| | A 紀錄筆數（PART 2 的圖）| B 相異神經元（PART 3 指定、PART 4／5 實跑）|
|---|---|---|
| v(r) 最大 / 平均 | 206.6 / 94.3 | 142.8 / 65.7 |
| 五數綜合 | 0.6/51.4/92.9/136.5/206.6 | 0.6/36.4/64.9/94.7/142.8 |
| 熱點體素 | 3,128 | 3,130 |
| 配對 / 相異距離值 | 4,890,628 / 573 | 4,896,885 / 557 |
| 高度 8 | 6 群 397/455/535/551/555/635 | 5 群 346/455/520/798/1011 |
| Spearman(A,B) | 0.9929 | |
| 熱點重疊（交集/聯集）| 93.7% | |

**PART 3：驗收表與指令自己指定的定義不一致，已依使用者裁決改成實測值。**
指令 4 白紙黑字要求「相異神經元數」，但驗收表給的是 A 欄的數字。
使用者選了「更新成實測值」，指令 4–8 共 19 處已改成 B 欄：

| 指令 | 原（A） | 新（B，實測） |
|---|---|---|
| 4 | 約 20 萬／約 149 GB／206.6 / 94.3 | 172,170／約 110 GB／142.8 / 65.7 |
| 5 | 0.6／51.4／92.9／136.5／206.6；3,128 | 0.6／36.4／64.9／94.7／142.8；3,130 |
| 6 | 6 群；397、455、535、551、555、635（3,128） | 5 群；346、455、520、798、1,011（3,130） |
| 7 | 4,890,628 / 573；高度 4 最大群 116；打亂九次 12,13,13,13,13,14,14,14,16；全範圍 0–16 | 4,896,885 / 557；114；13,13,13,14,14,14,15,15,15；0–15 |
| 8 | 4.8% / 35.8%；放寬 50.9% | 4.5% / 23.6%；放寬 46.9% |

**代價（要記著）**：PART 4「指令 4 的驗收表對不上」那段，讀者現在點過去
只會看到相符的數字。所以 PART 4 兩處改成「當時的驗收表寫⋯」，
並在該節加了一個註，說明實測值已經寫回 PART 3。
**PART 4 是流程頁（第 2 節的例外），保留這段歷史帳是刻意的；
但凡是引用另一頁「現在寫著什麼」的句子，都要標明時態。**

**PART 4：三處，已改。**

1. 「**PART 2 已經照這個發現更正。**」——不成立（見上）。已改成指向 PART 2 現在的寫法，
   並且真的把 PART 2 改掉了。
2. 「而 PART 2 明明寫著『**我們那張圖用的是重複註冊次數**』」——PART 2 沒有這句話，
   是把大意改寫後加上引號。**引用自家頁面也要逐字比對。**
3. Spearman 排序相關寫 **0.998**，實測 **0.993**。原本那個數字沒有留下算式，
   是憑印象寫的。

### 2026-09-04（第三十四次）拿到論文 PDF，跑引文稽核，抓到一處「論文沒說 X」講太寬

使用者給了 Chiang 2011 的 PDF 路徑（`research_with_AI/papers/CurrentBiology2011_…pdf`，
含補充材料）。

**先更正我自己在這次 session 說錯的一件事**：我在交付時說「這台機器上找不到論文 PDF，
所以引文沒辦法逐條對回原文」，語氣像是這些頁面從來沒有對過原文。**不是。**
第九次的紀錄寫著「十四條英文引文逐條對回原文（含雙欄還原）——全部相符」，
`scripts/quote_audit.py` 本身就是那時候為了這篇論文寫的（commit `10b030a`）。
**真正發生的是我這次沒找到檔案**：我用 `*chiang*2011*` 去 find，
而檔名是 `CurrentBiology2011_…`；而且我在同一輪已經看到
`research_with_AI/` 底下有 `chiang2011-fig*.jpg` 與 `s-chiang-2011-flycircuit.html`，
卻沒有再 `ls` 一次隔壁的 `papers/`。**下次找論文先列 `research_with_AI/papers/`。**

這次真正新增的是：把稽核範圍從當時的兩頁擴到**五頁 33 條**，並抓到下面那個錯。

**引文稽核：五頁 33 條英文引文全數對得上**，包含規範第 3 節點名的幾條高風險引文：
`Most anatomically demarcated neuropil regions…satisfied the criteria of an LPU`、
`the entire MB was one LPU because…`、`Each tract is composed of at least three neurons`
（確認只出現在圖 7D 的圖說，行 409）、`is an LPU if the region has its own long-range tracts`、
`with all voxel units enlarged 4 folds`、以及 v(r) 的兩種說法。

**同時順手驗掉的幾條「論文沒有給」**（都成立）：
`4 folds` 全文只出現 1 次；`isosurface` 0 次（原文是 *isointensity surface*）；
UPGMA 的切割高度全文沒有；四分位、80%、c>2 都沒有推導；
神經束的 `<30 voxels` 與 `>60% similarity` 與 PART 2／3 寫的相符。
c 的公式 c = (N_B/N_A)·(Σ_A v / Σ_B v) 也確認等於 mean(A)/mean(B)，與我們的實作一致。

**抓到的錯（第 3 節第四種變形：「論文沒說 X」的 X 講得太寬）：**

PART 2 的 STEP 4 第四點原本寫——

> **四、論文沒說「被撐大的是誰」。**是要撐大 source、target、還是兩邊都撐？
> `enlarged 4 folds` 這句話對此完全沒有交代

但論文的原句是 **With *all* voxel units enlarged 4 folds, a target neuron is recruited
if over 50% of *its* volume overlaps with the source.**——**「all」已經回答了「撐大誰」：兩邊都撐大。**
而且 **PART 4 自己就引了含 `all` 的完整句子**，等於站內兩頁互相矛盾。
更糟的是，item 四把引文截成 `enlarged 4 folds`，**剛好截掉了那個關鍵字**。

真正沒交代的窄很多：`over 50% of **its** volume` 的分母，是 target 撐大前還是撐大後。
已改寫成先寫論文有說的、再寫沒說的；四列表格重新定位（第四列才是字面讀法，
0.232／4.0%），並明說本頁與 PART 4 用的「只撐大 source」是**我們的簡化**，
理由是滾雪球時 source 一直在長大。PART 3 指令 9 的同一句、
以及 PART 2 收尾「四個定義上的含糊」表的那一列，一併縮窄。

**教訓**：截短引文時要問一次「我截掉的字有沒有正在回答我說『論文沒回答』的那個問題」。

### 2026-09-04（第三十五次）PART 5 用論文再對一次：步驟編號整組錯位一格

PART 5 沒有英文引文（稽核 33 條全在 PART 1–4），所以這次對的是**它怎麼引用步驟編號**。

**發現：PART 5 的「步驟 N」整組比 PART 2 多一。**

`lpu_lib.py` 自己把「數密度」與「3×3×3 平滑」拆成兩步（1 和 2），
PART 2 則把兩件事合在 STEP 1 裡。於是從熱點開始全部錯開：

| 這一步 | PART 2／PART 3（正確） | PART 5 原本寫 |
|---|---|---|
| 3×3×3 平滑 | STEP 1 | 步驟 2 |
| 取上四分位 | STEP 2 | 步驟 3 |
| 1% 過濾 | STEP 3 | 步驟 4 |
| 50% 招募 | STEP 4 | 步驟 5 |
| 中心 80%、c>2 | STEP 6 | 步驟 7 |

而 PART 5 那張對照表的表頭寫的正是「**PART 2 的步驟**」——**標籤說是 PART 2 的，
內容卻是函式庫自己的一套。** PART 3 每一條指令的 `對應 PART 2：STEP n` 都是對的，
只有 PART 5 錯，所以是 PART 5 改。

同一張表還**整列漏掉 STEP 7（長程神經束驗證）**：指令 12 被歸進「（收尾）相鄰與合併」，
但它其實是 STEP 7 的降級版。已拆成兩列（12 → STEP 7 `adjacency`、13 → 步驟以外 `merge_gain`）。

`lpu_lib.py` 的 Config 註解與七個章節標題一律改用 `STEP n`（跟 PART 2 同一套字），
不再用「步驟 n」，免得兩套編號再混。

**順帶用論文對掉三處 docstring**：

1. `recruit`：原本寫「只撐大 source，不撐大 target，**否則兩邊都撐大會算出超過 1 的比例**」
   ——這句把兩件事混成一件。兩邊都撐大而**分母用撐大後**的體積是好好的 0.232；
   算出超過 1 的是「兩邊都撐大但分母用撐大**前**」。已改成與第三十四次一致的寫法。
2. `pick_seed`：原本寫「纖維侷限在該候選 LPU 內」，論文是
   *a smallest LN with its fibers **restricted within** the **smallest** candidate LPU*
   ——「完全侷限」且是「**最小的**那個候選 LPU」。已補上原句。
3. `density_field`：原本只寫「論文寫的是纖維條數」，漏掉圖 S5D 的第二種說法，
   而我們實作的正是第二種。已把兩種說法都寫進去。

重跑驗證：166 秒、47/47 通過、判定 8／13／54 不變。`lpu_lib.py` 553 → 561 行，
頁面與 README 的行數同步更新。
